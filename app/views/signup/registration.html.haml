-# 会員情報入力
.menberInfo__input

  = render "shared/users/headers"
  
  %main.single-main
    %section.l-single-container
      %h2.l-single-head.registration 会員情報入力
      = form_with model: @user, local: true, url: sms_confirmation_signup_index_path, class:"l-single-inner registration-form", method: :get do |f|
        .l-single-content
          .form-group
            = f.label :nickname do
              ニックネーム
            %span.form-require 必須
            = f.text_field :nickname, placeholder: "例）メルカリ太朗", class:"input-default"
          .form-group
            = f.label :email do
              メールアドレス
            %span.form-require 必須
            = f.text_field :email, placeholder: "PC・携帯どちらでも可", class:"input-default"
            .form-group.form-password-revelation
              = f.label :password do
                パスワード
              %span.form-require 必須
              = f.text_field :password, placeholder: "7文字以上の半角英数字", class:"input-default"
              %p.form-info-text
                ※ 英字と数字の両方を含めて設定してください
              .form-password-revelation-toggle
                .checkbox-default
                  %i.icon-check
                  -# 後で実装
                  -# = f.label :reveal_password do
                  -#   パスワードを表示する
                .form-password-revelation-revealed-password-container
                  %span.form-password-revelation-revealed-password
            .form-group
              %h3.text-left.l-chapter-sub-head 本人確認
              %p.l-single-text
                安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
            .form-group
              %div
                = f.label :family_name_kanji do
                  お名前(全角)
                %span.form-require 必須
              = f.text_field :family_name_kanji, placeholder: "例) 山田", class:"input-default"
              = f.text_field :first_name_kanji, placeholder: "例) 彩", class:"input-default"
            .form-group
              %div
                = f.label :family_name_kana do
                  お名前カナ(全角)
                %span.form-require 必須
              = f.text_field :family_name_kana, placeholder: "例) ヤマダ", class:"input-default"
              = f.text_field :first_name_kana, placeholder: "例) アヤ", class:"input-default"
            .form-group
              = f.label :birthdata_year do
                生年月日
              %span.form-require 必須
              %br/
              .birthday-select-wrap
                .select-wrap
                  %i.icon-arrow-bottom
                  = f.select :birthdata_year, [*1900..2019].reverse, { prompt: "--"}, class:"select-default"
                %span_span 年
                .select-wrap.select-wrap__month
                  %i.icon-arrow-bottom
                  = f.select :birthdata_month, [*1..12], { prompt: "--"}, class:"select-default" 
                %span_span 月
                .select-wrap.select-wrap__day
                  %i.icon-arrow-bottom
                  = f.select :birthdata_day, [*1..31], { prompt: "--"}, class:"select-default"
                %span_span 日
              -# %input{name: "birthday", type: "hidden", value: ""}/
              .clearfix
            %p.form-info-text
              ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          .l-single-content
            -# %input{name: "after_save_callback", type: "hidden", value: ""}/
            .form-group
              %p.l-single-text.text-center
                「次へ進む」のボタンを押すことにより、
                =link_to "#" do
                  利用規約
                に同意したものとみなします
                = f.submit value: "次へ進む", class:"btn-default btn-red"
              .form-group.text-right
              %p
                =link_to "#" do
                  本人情報の登録について
                  %i.icon-arrow-right
  = render "shared/users/footers"